#include <bits/stdc++.h>
using namespace std;

int main() {
    ios::sync_with_stdio(0);
    cin.tie(0);

    int q;
    string op;
    int x;
    multiset<int> ms;
    while (q--) {
        cin >> op >> x;
        if 
    }


    return 0;
}