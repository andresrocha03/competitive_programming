# Notes

- Difference array: an array to represent how an array changes without storing the final values directly. Actually  the element a[x] from an array is given by $ a_i = \sum_{i=0}^x diff[i] $, a nice application is count interval coverage of each position in the array.